<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Track-App Promo V2 - Fixed Layout</title>
<link href="https://fonts.googleapis.com/css2?family=Lexend:wght@400;500;600;700;800&family=Space+Grotesk:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
<script src="https://cdn.tailwindcss.com"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/TextPlugin.min.js"></script>
<link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
<script>
  tailwind.config = {
    theme: {
      extend: {
        colors: {
          'safety-orange': '#F97316',
          'electric-blue': '#3B82F6',
          'paypal': '#003087',
          'shopify': '#96bf48',
        },
        fontFamily: {
          heading: ['Space Grotesk', 'sans-serif'],
          sans: ['Lexend', 'sans-serif']
        }
      }
    }
  }
</script>
<style>
  body { background: #000; display: flex; justify-content: center; align-items: center; height: 100vh; margin: 0; overflow: hidden; }
  .wrapper { height: 95vh; aspect-ratio: 9/16; position: relative; border-radius: 24px; overflow: hidden; background: #0f172a; border: 1px solid rgba(255,255,255,0.1); }
  .comp { position: absolute; top: 0; left: 0; width: 1080px; height: 1920px; transform-origin: top left; }
  
  .scene { position: absolute; inset: 0; opacity: 0; display: none; flex-direction: column; }
  .scene.active { display: flex; opacity: 1; }

  /* Giant TikTok Text */
  .tiktok-text {
    font-size: 120px; font-weight: 900; font-family: 'Space Grotesk';
    color: white; text-transform: uppercase; line-height: 1.1;
    text-align: center; text-shadow: 0 20px 50px rgba(0,0,0,0.9);
    position: absolute; z-index: 100; width: 100%; padding: 0 40px;
  }
  
  .highlight-bg { background: var(--tw-colors-safety-orange); padding: 10px 40px; border-radius: 30px; display: inline-block; transform: rotate(-2deg); }

  /* Map background overlay */
  .map-overlay {
    position: absolute; inset: 0; background: rgba(241, 245, 249, 0.5); pointer-events: none; z-index: 10;
  }
  .leaflet-container {
    background: #f1f5f9;
  }
  .custom-marker {
    background: #3B82F6; border: 4px solid white; border-radius: 50%; box-shadow: 0 0 15px rgba(59,130,246,0.5);
  }
  .dest-marker {
    background: #F97316; border: 4px solid white; border-radius: 50%; box-shadow: 0 0 20px rgba(249,115,22,0.8);
  }

  .glass-card {
    background: rgba(255, 255, 255, 0.95); backdrop-filter: blur(20px);
    border: 4px solid rgba(255,255,255,0.8); border-radius: 60px;
    box-shadow: 0 40px 100px rgba(0,0,0,0.5);
  }
</style>
</head>
<body class="antialiased font-sans">

<div class="wrapper" id="wrapper">
  <div class="comp" id="comp">
    <!-- Background grid -->
    <div class="absolute inset-0 bg-slate-900 opacity-5" style="background-image: radial-gradient(#fff 2px, transparent 2px); background-size: 40px 40px;"></div>

    <!-- SCENE 1: THE PAIN (0 - 4s) -->
    <div class="scene active z-10" id="s1">
      <div class="absolute inset-0 bg-red-900/40"></div>
      
      <!-- Hook Text -->
      <div class="tiktok-text top-[10%]" id="t-dead">
        DROPSHIPPING IS<br><span class="text-red-500 text-[160px] drop-shadow-[0_0_40px_rgba(239,68,68,1)]">DEAD?</span>
      </div>

      <!-- Centered Popups Container -->
      <div class="absolute top-[35%] w-full h-[600px] flex items-center justify-center">
        <!-- PayPal Hold -->
        <div class="absolute w-[90%] mb-[380px] bg-white rounded-[50px] p-12 border-8 border-red-500 opacity-0 scale-50 z-30 shadow-[0_30px_100px_rgba(220,38,38,0.8)]" id="pop-paypal">
          <div class="flex items-center justify-between mb-8">
            <div class="text-paypal font-black italic text-7xl">PayPal</div>
            <div class="text-red-600 font-bold text-4xl px-6 py-3 bg-red-50 rounded-full">ALERT</div>
          </div>
          <h2 class="text-[80px] font-black text-slate-900 leading-none mb-4">FUNDS ON HOLD</h2>
          <p class="text-[100px] font-bold text-red-600 leading-none">-$4,500.00</p>
        </div>

        <!-- Shopify Chargeback -->
        <div class="absolute w-[95%] bg-white rounded-[50px] p-12 opacity-0 scale-50 z-40 shadow-[0_30px_100px_rgba(0,0,0,0.8)] border-4 border-slate-200 mt-[350px]" id="pop-shop" style="transform: rotate(3deg);">
          <div class="flex items-center gap-6 mb-6">
            <div class="w-16 h-16 bg-shopify rounded-full"></div>
            <span class="text-5xl font-bold text-slate-500">Shopify Payments</span>
          </div>
          <h2 class="text-[80px] font-black text-slate-900 leading-none">Chargeback Initiated</h2>
          <p class="text-5xl text-slate-600 mt-4 font-bold text-red-500">Item not received.</p>
        </div>
      </div>

      <!-- Boring 17track style (Fills entire screen, covers everything) -->
      <div class="absolute inset-0 bg-white z-50 opacity-0 flex flex-col justify-center px-10" id="s1-boring">
        <h2 class="text-[80px] font-black text-slate-900 mb-12 text-center">Standard Tracking</h2>
        <div class="w-full bg-slate-50 p-16 rounded-[60px] border-4 border-slate-200">
          <div class="flex flex-col gap-12">
            <div class="flex gap-8 text-5xl text-slate-500">
              <span class="font-bold whitespace-nowrap">Oct 12</span>
              <span class="font-bold text-slate-800">[CHINA] Package handed to airline.</span>
            </div>
            <div class="flex gap-8 text-5xl text-slate-400">
              <span class="font-bold whitespace-nowrap">Oct 11</span>
              <span>[CHINA] Arrived at sorting center.</span>
            </div>
          </div>
          <div class="mt-24 bg-red-50 text-red-600 p-12 rounded-3xl text-[70px] font-black text-center border-4 border-red-200 uppercase">
            No updates for 14 days
          </div>
        </div>
        <div class="tiktok-text top-[75%] text-[90px]">
          <span class="bg-red-600 px-10 py-4 rounded-[40px] text-white inline-block mb-6">Customers refund</span><br>
          <span class="text-slate-900">when tracking freezes.</span>
        </div>
      </div>
    </div>

    <!-- SCENE 2: The Secret Weapon -->
    <div class="scene z-10 bg-slate-100" id="s2">
      <div class="tiktok-text top-[15%] text-slate-900" id="t-secret">
        The Secret:<br><span class="highlight-bg text-white mt-4">Generate Movement.</span>
      </div>

      <div class="flex-1 flex flex-col items-center justify-center w-full px-10">
        <div class="glass-card w-[95%] p-16 mt-[450px]" id="s2-card">
          <div class="flex items-center gap-8 mb-16">
            <div class="w-28 h-28 bg-safety-orange rounded-3xl flex items-center justify-center shadow-lg shadow-orange-500/50">
              <svg class="w-16 h-16 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="3"><path stroke-linecap="round" stroke-linejoin="round" d="M13 10V3L4 14h7v7l9-11h-7z" /></svg>
            </div>
            <h2 class="text-[80px] font-black font-heading text-slate-900 leading-none">Track-App Admin</h2>
          </div>

          <div class="bg-slate-50 border-4 border-slate-200 rounded-[40px] p-12 mb-12">
            <label class="block text-4xl font-bold text-slate-500 uppercase tracking-widest mb-6">Route Simulation</label>
            <div class="flex justify-between items-center bg-white border-4 border-slate-200 p-10 rounded-3xl text-5xl font-black text-slate-900">
              <span>Origin: China</span>
              <span class="text-electric-blue">Dest: Frankfurt</span>
            </div>
          </div>

          <div id="btn-generate" class="w-full bg-safety-orange text-white text-[70px] font-black font-heading uppercase tracking-wider py-12 rounded-[40px] text-center shadow-[0_30px_60px_rgba(249,115,22,0.4)]">
            GENERATE
          </div>
        </div>
      </div>
    </div>

    <!-- SCENE 3: The Magic -->
    <div class="scene z-10 bg-slate-100" id="s3">
      <!-- Real Leaflet Map -->
      <div id="map" class="absolute inset-0 z-0 h-[1100px] border-b-4 border-slate-300"></div>
      <div class="map-overlay"></div>

      <div class="tiktok-text top-[5%] text-slate-900 z-50 drop-shadow-2xl" id="t-magic">
        They stay calm.<br><span class="highlight-bg text-white mt-4">You keep the money.</span>
      </div>

      <!-- Tracking Hero -->
      <div class="absolute bottom-10 w-full px-10 z-20">
        <div class="glass-card w-full p-16 bg-white" id="hero-card">
          <div class="flex justify-between items-center mb-16">
            <div class="flex-1 pr-8">
              <div class="text-4xl text-slate-500 font-bold font-mono mb-4">
                TID-US8199
              </div>
              <h1 id="live-status" class="text-[70px] font-black font-heading text-slate-900 leading-tight">
                Pending
              </h1>
            </div>
            <div class="text-right bg-blue-50 p-8 rounded-[40px] border-4 border-blue-100 max-w-[400px]" id="est-box">
              <div class="text-2xl font-extrabold uppercase text-electric-blue mb-2">Estimated Delivery</div>
              <div class="text-5xl font-black text-slate-900">Arriving Tomorrow</div>
            </div>
          </div>

          <!-- Progress Bar -->
          <div class="relative mt-[120px] px-6">
            <div class="absolute top-8 left-0 right-0 h-6 bg-slate-200 rounded-full">
              <div id="progress-fill" class="h-full w-0 bg-safety-orange rounded-full shadow-[0_0_30px_rgba(249,115,22,0.6)]"></div>
            </div>
            <div class="flex justify-between relative z-10">
              <div class="w-20 h-20 rounded-full bg-safety-orange border-[8px] border-safety-orange flex items-center justify-center text-white"><svg class="w-10 h-10" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="4" stroke-linecap="round"><polyline points="20 6 9 17 4 12"></polyline></svg></div>
              <div id="node2" class="w-20 h-20 rounded-full bg-white border-[8px] border-slate-300"></div>
              <div id="node3" class="w-20 h-20 rounded-full bg-white border-[8px] border-slate-300"></div>
              <div id="node4" class="w-20 h-20 rounded-full bg-white border-[8px] border-slate-300"></div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- SCENE 4: Outro -->
    <div class="scene z-10" id="s4">
      <div class="absolute inset-0 bg-slate-900"></div>
      
      <div class="flex-1 flex flex-col items-center justify-center w-full p-20 text-center z-10">
        <div id="logo-final" class="w-[400px] h-[400px] bg-safety-orange rounded-[100px] flex items-center justify-center mb-20 shadow-[0_50px_150px_rgba(249,115,22,0.5)]">
          <svg class="w-[200px] h-[200px] text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="3"><path stroke-linecap="round" stroke-linejoin="round" d="M13 10V3L4 14h7v7l9-11h-7z" /></svg>
        </div>
        <h1 class="text-[180px] font-black font-heading text-white leading-none tracking-tighter mb-10">Track-App</h1>
        <p class="text-[70px] font-bold text-electric-blue uppercase tracking-widest mt-10 leading-relaxed text-center">
          The Dropshipper's<br><span class="text-white">Cheat Code.</span>
        </p>
      </div>
    </div>

  </div>
</div>

<script>
function resize() {
  const wrapper = document.getElementById('wrapper');
  const comp = document.getElementById('comp');
  const scale = wrapper.clientHeight / 1920;
  comp.style.transform = `scale(${scale})`;
}
window.addEventListener('resize', resize);
resize();

gsap.registerPlugin(TextPlugin);

// Map Initialization
let map, marker, routeLine;
function initMap() {
  // Start centered on China, zoomed out enough to see movement
  map = L.map('map', { zoomControl: false, attributionControl: false }).setView([22.5431, 114.0579], 3);
  L.tileLayer('https://{s}.basemaps.cartocdn.com/light_nolabels/{z}/{x}/{y}{r}.png').addTo(map);
  
  // Origin (Shenzhen, China) and Dest (Frankfurt, Germany)
  const origin = [22.5431, 114.0579];
  const dest = [50.1109, 8.6821];
  
  L.marker(dest, { icon: L.divIcon({ className: 'dest-marker', iconSize: [40, 40] }) }).addTo(map);
  marker = L.marker(origin, { icon: L.divIcon({ className: 'custom-marker', iconSize: [30, 30] }) }).addTo(map);
  
  // Create line
  routeLine = L.polyline([origin, origin], { color: '#F97316', weight: 8, dashArray: '15, 15', lineCap: 'round' }).addTo(map);
}
initMap();

const tl = gsap.timeline({ repeat: -1, repeatDelay: 2 });
gsap.set('.scene', { display:'none', opacity:0 });

// SCENE 1 (0-4s)
tl.set('#s1', { display:'flex', opacity:1 })
  .from('#t-dead', { scale: 1.5, opacity: 0, duration: 0.4, ease: 'back.out(2)' })
  .to('#pop-paypal', { scale: 1, opacity: 1, duration: 0.3, ease: 'back.out(2)' }, "+=0.3")
  .to('#pop-shop', { scale: 1, rotation: 3, opacity: 1, duration: 0.3, ease: 'back.out(2)' }, "+=0.2")
  // Show 17track view and fade out everything underneath it
  .to(['#t-dead', '#pop-paypal', '#pop-shop'], { opacity: 0, duration: 0.3 }, "+=1.5")
  .to('#s1-boring', { opacity: 1, duration: 0.3 }, "<")
  .to({}, { duration: 2.5 }); // Keep 17track view on screen

// SCENE 2 (4s-8s)
tl.set('#s1', { display: 'none' })
  .set('#s2', { display: 'flex', opacity: 1 })
  .from('#t-secret', { y: -100, opacity: 0, duration: 0.5, ease: 'back.out' })
  .from('#s2-card', { y: 200, opacity: 0, duration: 0.6, ease: 'power3.out' }, "-=0.3")
  .to('#btn-generate', { scale: 0.95, background: '#ea580c', duration: 0.1, yoyo: true, repeat: 1 }, "+=1.5")
  .to({}, { duration: 2.5 }); // Keep Scene 2 on screen

// SCENE 3 (8s-13s)
tl.set('#s2', { display: 'none' })
  .set('#s3', { display: 'flex', opacity: 1 })
  .call(() => {
    // Leaflet map needs to recalculate size after its container becomes visible
    if(map) {
      setTimeout(() => map.invalidateSize(), 100);
    }
  })
  .from('#t-magic', { y: -100, opacity: 0, duration: 0.5, ease: 'back.out' })
  .from('#hero-card', { y: 300, opacity: 0, duration: 0.5, ease: 'power3.out' }, "-=0.3")
  // Update status
  .to('#live-status', { text: "Out for Delivery", color: "#F97316", duration: 0.6, ease: 'none' }, "+=0.5")
  .to('#est-box', { scale: 1.05, backgroundColor: "#e0f2fe", borderColor: "#bae6fd", duration: 0.5, ease: 'back.out(2)' }, "<")
  // Progress bar
  .add("fillStart", "+=0.2")
  .to('#progress-fill', { width: "100%", duration: 1.5, ease: 'power2.inOut' }, "fillStart")
  .to('#node2', { backgroundColor: "#F97316", borderColor: "#F97316", duration: 0.2 }, "fillStart+=0.4")
  .to('#node3', { backgroundColor: "#F97316", borderColor: "#F97316", duration: 0.2 }, "fillStart+=0.9")
  .to('#node4', { backgroundColor: "#F97316", borderColor: "#F97316", boxShadow: "0 0 30px rgba(249,115,22,0.6)", duration: 0.2 }, "fillStart+=1.4")
  // Map Animation (Leaflet)
  .to({}, { 
    duration: 2.5, 
    ease: "power2.inOut",
    onUpdate: function() {
      const p = this.progress();
      const startLat = 22.5431;
      const startLng = 114.0579;
      const endLat = 50.1109;
      const endLng = 8.6821;
      
      const lat = startLat + (endLat - startLat) * p;
      const lng = startLng + (endLng - startLng) * p;
      marker.setLatLng([lat, lng]);
      routeLine.setLatLngs([[startLat, startLng], [lat, lng]]);
      if (p > 0.1) map.panTo([lat, lng], { animate: false });
    }
  }, "-=1.5")
  .to({}, { duration: 2 }); // Keep scene 3 on screen for 2 more seconds!

// SCENE 4 (13s-15s)
tl.set('#s3', { display: 'none' })
  .set('#s4', { display: 'flex', opacity: 1 })
  .from('#logo-final', { scale: 0, rotation: -90, duration: 0.8, ease: 'back.out(2)' })
  .from('h1', { opacity: 0, y: 80, duration: 0.5 }, "-=0.4")
  .from('p', { opacity: 0, y: 50, duration: 0.5 }, "-=0.3");
</script>
</body>
</html>
